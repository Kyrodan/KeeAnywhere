namespace KeeAnywhere.StorageProviders
{
    public enum StorageType
    {
        // Preserve sort order: ascending!
        Dropbox = 0,
        DropboxRestricted = 4,
        GoogleDrive = 1,
        HubiC = 2,
        OneDrive = 3,
    }
}